name: Release Production

on:
    push:
        tags:
            - "v*"

permissions:
    contents: write
    id-token: write # Required for SignPath

jobs:
    create-release:
        runs-on: ubuntu-latest
        outputs:
            release_id: ${{ steps.create_release.outputs.id }}

        steps:
            - name: Checkout
              uses: actions/checkout@v4
              with:
                  fetch-depth: 0

            - name: Generate release notes
              id: notes
              shell: bash
              run: |
                  CURRENT_TAG="${GITHUB_REF#refs/tags/}"
                  PREV_TAG=$(git tag --sort=-version:refname | grep -v "^${CURRENT_TAG}$" | head -n 1 || true)

                  echo "Current tag: $CURRENT_TAG"
                  echo "Previous tag: $PREV_TAG"

                  if [ -z "$PREV_TAG" ]; then
                    RAW_COMMITS=$(git log --pretty=format:"%s|%h|%an" HEAD)
                  else
                    RAW_COMMITS=$(git log --pretty=format:"%s|%h|%an" "${PREV_TAG}..HEAD")
                  fi

                  FEATURES=""
                  FIXES=""
                  IMPROVEMENTS=""
                  OTHER=""

                  while IFS='|' read -r subject hash author; do
                    [ -z "$subject" ] && continue
                    ENTRY="- ${subject} (\`${hash}\`) — *${author}*"
                    lower=$(echo "$subject" | tr '[:upper:]' '[:lower:]')
                    if echo "$lower" | grep -qE "^(feat|add|new|feature)"; then
                      FEATURES="${FEATURES}${ENTRY}"$'\n'
                    elif echo "$lower" | grep -qE "^(fix|bug|hotfix|patch|revert)"; then
                      FIXES="${FIXES}${ENTRY}"$'\n'
                    elif echo "$lower" | grep -qE "^(improve|enhance|update|refactor|perf|chore|style|docs|test|ci|build)"; then
                      IMPROVEMENTS="${IMPROVEMENTS}${ENTRY}"$'\n'
                    else
                      OTHER="${OTHER}${ENTRY}"$'\n'
                    fi
                  done <<< "$RAW_COMMITS"

                  TOTAL=$(echo "$RAW_COMMITS" | grep -c '.' || true)
                  COMPARE_URL="https://github.com/${{ github.repository }}/compare/${PREV_TAG}...${CURRENT_TAG}"

                  {
                    echo "RELEASE_BODY<<ENDOFBODY"
                    echo "## Viezan ${CURRENT_TAG} — Release Notes"
                    echo ""
                    echo "> **Viezan** is a local AI-powered translation desktop app for macOS and Windows,"
                    echo "> supporting models from OpenAI, Anthropic Claude, and Google Gemini."
                    echo ""
                    echo "---"
                    echo ""

                    if [ -n "$FEATURES" ]; then
                      echo "### ✨ New Features"
                      echo ""
                      echo "$FEATURES"
                    fi

                    if [ -n "$FIXES" ]; then
                      echo "### 🐛 Bug Fixes"
                      echo ""
                      echo "$FIXES"
                    fi

                    if [ -n "$IMPROVEMENTS" ]; then
                      echo "### 🔧 Improvements & Maintenance"
                      echo ""
                      echo "$IMPROVEMENTS"
                    fi

                    if [ -n "$OTHER" ]; then
                      echo "### 📝 Other Changes"
                      echo ""
                      echo "$OTHER"
                    fi

                    if [ "$TOTAL" -eq 0 ]; then
                      echo "### 📋 Changes"
                      echo ""
                      echo "- Initial release or no conventional commits found."
                      echo ""
                    fi

                    echo "---"
                    echo ""
                    echo "## 📦 Installation"
                    echo ""
                    echo "###  macOS"
                    echo "1. Download the \`.dmg\` file matching your chip:"
                    echo "   - **Apple Silicon (M1/M2/M3):** \`Viezan-*-arm64.dmg\`"
                    echo "   - **Intel Mac:** \`Viezan-*-x64.dmg\`"
                    echo "2. Open the \`.dmg\` and drag **Viezan** to your **Applications** folder."
                    echo "3. On first launch, right-click → **Open** to bypass Gatekeeper if needed."
                    echo ""
                    echo "### ⊞ Windows"
                    echo "1. Download \`Viezan-*-x64-Setup.exe\`."
                    echo "2. Run the installer and follow the setup wizard."
                    echo "3. You can choose a custom installation directory during setup."
                    echo ""
                    echo "### 🐧 Linux"
                    echo "1. Download the \`.AppImage\` or \`.deb\` file."
                    echo "2. For AppImage: \`chmod +x Viezan-*.AppImage && ./Viezan-*.AppImage\`"
                    echo "3. For .deb: \`sudo dpkg -i Viezan-*.deb\`"
                    echo ""
                    echo "---"
                    echo ""
                    if [ -n "$PREV_TAG" ]; then
                      echo "**📜 Full Changelog:** [${PREV_TAG} → ${CURRENT_TAG}](${COMPARE_URL})"
                    else
                      echo "**📜 Full Changelog:** [View all commits](https://github.com/${{ github.repository }}/commits/${CURRENT_TAG})"
                    fi
                    echo "ENDOFBODY"
                  } >> "$GITHUB_OUTPUT"

                  echo "PREV_TAG=$PREV_TAG" >> "$GITHUB_OUTPUT"
                  echo "CURRENT_TAG=$CURRENT_TAG" >> "$GITHUB_OUTPUT"

            - name: Create GitHub Release
              id: create_release
              uses: softprops/action-gh-release@v2
              with:
                  name: "Viezan ${{ steps.notes.outputs.CURRENT_TAG }}"
                  body: ${{ steps.notes.outputs.RELEASE_BODY }}
                  draft: false
                  prerelease: ${{ contains(github.ref, '-beta') || contains(github.ref, '-alpha') || contains(github.ref, '-rc') }}
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    # ─────────────────────────────────────────────
    # macOS build — Apple Silicon (M1/M2/M3)
    # ─────────────────────────────────────────────
    build-mac-arm64:
        needs: create-release
        runs-on: macos-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup Node
              uses: actions/setup-node@v4
              with:
                  node-version: 22
                  cache: npm

            - name: Update version in package.json
              shell: bash
              run: |
                  VERSION="${GITHUB_REF#refs/tags/v}"
                  CURRENT=$(node -p "require('./package.json').version")
                  if [ "$CURRENT" != "$VERSION" ]; then
                    echo "Updating package.json from $CURRENT to $VERSION"
                    npm version "$VERSION" --no-git-tag-version
                  else
                    echo "package.json already at version $VERSION, skipping."
                  fi

            - name: Install dependencies
              run: npm ci

            - name: Build
              run: npm run build

            - name: Build and publish (macOS arm64)
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                  CSC_IDENTITY_AUTO_DISCOVERY: false
                  APPLE_ID: ${{ secrets.APPLE_ID }}
                  APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
                  APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
              run: npx electron-builder --mac --arm64 --publish always

    # ─────────────────────────────────────────────
    # macOS build — Intel (x64)
    # ─────────────────────────────────────────────
    build-mac-intel:
        needs: create-release
        runs-on: macos-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup Node
              uses: actions/setup-node@v4
              with:
                  node-version: 22
                  cache: npm

            - name: Update version in package.json
              shell: bash
              run: |
                  VERSION="${GITHUB_REF#refs/tags/v}"
                  CURRENT=$(node -p "require('./package.json').version")
                  if [ "$CURRENT" != "$VERSION" ]; then
                    echo "Updating package.json from $CURRENT to $VERSION"
                    npm version "$VERSION" --no-git-tag-version
                  else
                    echo "package.json already at version $VERSION, skipping."
                  fi

            - name: Install dependencies
              run: npm ci

            - name: Build
              run: npm run build

            - name: Build and publish (macOS x64)
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                  CSC_IDENTITY_AUTO_DISCOVERY: false
                  APPLE_ID: ${{ secrets.APPLE_ID }}
                  APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
                  APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
              run: npx electron-builder --mac --x64 --publish always

    # ─────────────────────────────────────────────
    # Windows build → SignPath signing (free for OSS, optional)
    # If SIGNPATH secrets are not configured, unsigned installer is
    # uploaded directly. Set up SignPath to get a trusted signature:
    #   1. Register at https://signpath.io → "Open Source" free plan
    #   2. Link your GitHub repo in SignPath project settings
    #   3. Create project slug: "tre-assistant"
    #   4. Create signing policy slug: "release-signing"
    #   5. Create artifact config slug: "windows-installer"
    #   6. Add SIGNPATH_API_TOKEN & SIGNPATH_ORGANIZATION_ID as repo secrets
    # ─────────────────────────────────────────────
    build-windows:
        needs: create-release
        runs-on: windows-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup Node
              uses: actions/setup-node@v4
              with:
                  node-version: 22
                  cache: npm

            - name: Update version in package.json
              shell: bash
              run: |
                  VERSION="${GITHUB_REF#refs/tags/v}"
                  CURRENT=$(node -p "require('./package.json').version")
                  if [ "$CURRENT" != "$VERSION" ]; then
                    echo "Updating package.json from $CURRENT to $VERSION"
                    npm version "$VERSION" --no-git-tag-version
                  else
                    echo "package.json already at version $VERSION, skipping."
                  fi

            - name: Install dependencies
              run: npm ci

            - name: Build
              run: npm run build

            - name: Build unsigned Windows installer
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                  CSC_IDENTITY_AUTO_DISCOVERY: "false"
                  WIN_CSC_LINK: ""
                  WIN_CSC_KEY_PASSWORD: ""
              run: npx electron-builder --win nsis --x64 --publish never

            - name: Locate unsigned installer & app files
              id: find_installer
              shell: bash
              run: |
                  INSTALLER=$(ls release/*.exe | head -n 1)
                  echo "INSTALLER_PATH=$INSTALLER" >> "$GITHUB_OUTPUT"
                  echo "INSTALLER_NAME=$(basename $INSTALLER)" >> "$GITHUB_OUTPUT"
                  echo "Found installer: $INSTALLER"

            - name: Check if SignPath secrets are configured
              id: check_signpath
              shell: bash
              run: |
                  if [ -n "$SIGNPATH_API_TOKEN" ] && [ -n "$SIGNPATH_ORG_ID" ]; then
                    echo "ENABLED=true" >> "$GITHUB_OUTPUT"
                    echo "SignPath secrets found — will attempt signing."
                  else
                    echo "ENABLED=false" >> "$GITHUB_OUTPUT"
                    echo "SignPath secrets not configured — will upload unsigned installer."
                  fi
              env:
                  SIGNPATH_API_TOKEN: ${{ secrets.SIGNPATH_API_TOKEN }}
                  SIGNPATH_ORG_ID: ${{ secrets.SIGNPATH_ORGANIZATION_ID }}

            # ── SignPath signing (only when secrets are configured) ──────
            - name: Package installer + app PE files into ZIP for SignPath
              if: steps.check_signpath.outputs.ENABLED == 'true'
              shell: pwsh
              run: |
                  New-Item -ItemType Directory -Force -Path signing-input | Out-Null
                  Copy-Item release\*.exe signing-input\
                  if (Test-Path release\win-unpacked) {
                    Get-ChildItem -Path release\win-unpacked -Filter *.exe | Copy-Item -Destination signing-input\
                    Get-ChildItem -Path release\win-unpacked -Filter *.dll | Copy-Item -Destination signing-input\
                    Write-Host "Copied PE files:"
                    Get-ChildItem signing-input | Select-Object Name
                  }
                  Push-Location signing-input
                  Compress-Archive -Path * -DestinationPath ..\unsigned-bundle.zip -Force
                  Pop-Location
                  $size = (Get-Item unsigned-bundle.zip).Length / 1MB
                  Write-Host "Bundle size: $([math]::Round($size, 2)) MB"

            - name: Upload unsigned bundle as workflow artifact
              id: upload_unsigned
              if: steps.check_signpath.outputs.ENABLED == 'true'
              uses: actions/upload-artifact@v4
              with:
                  name: unsigned-windows-bundle
                  path: unsigned-bundle.zip
                  retention-days: 1

            - name: Sign bundle via SignPath.io
              id: sign
              if: steps.check_signpath.outputs.ENABLED == 'true'
              continue-on-error: true
              uses: signpath/github-action-submit-signing-request@v1
              with:
                  api-token: "${{ secrets.SIGNPATH_API_TOKEN }}"
                  organization-id: "${{ secrets.SIGNPATH_ORGANIZATION_ID }}"
                  project-slug: "tre-assistant"
                  signing-policy-slug: "release-signing"
                  artifact-configuration-slug: "windows-installer"
                  github-artifact-id: "${{ steps.upload_unsigned.outputs.artifact-id }}"
                  wait-for-completion: true
                  output-artifact-directory: "release-signed"

            - name: Extract signed installer and upload to GitHub Release
              if: steps.check_signpath.outputs.ENABLED == 'true' && steps.sign.outcome == 'success'
              shell: bash
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              run: |
                  TAG="${GITHUB_REF#refs/tags/}"
                  cd release-signed
                  SIGNED_ZIP=$(ls *.zip | head -n 1)
                  unzip -o "$SIGNED_ZIP" -d unpacked-signed
                  SIGNED_EXE=$(ls unpacked-signed/*Setup*.exe unpacked-signed/*setup*.exe 2>/dev/null | head -n 1)
                  if [ -z "$SIGNED_EXE" ]; then
                    SIGNED_EXE=$(ls unpacked-signed/*.exe | head -n 1)
                  fi
                  INSTALLER_NAME="${{ steps.find_installer.outputs.INSTALLER_NAME }}"
                  cp "$SIGNED_EXE" "../release-final-${INSTALLER_NAME}"
                  cd ..
                  echo "Uploading signed: release-final-${INSTALLER_NAME}"
                  gh release upload "$TAG" "release-final-${INSTALLER_NAME}" --clobber

            # ── Fallback: upload unsigned when SignPath is not configured or failed ──
            - name: Upload unsigned installer to GitHub Release (fallback)
              if: steps.check_signpath.outputs.ENABLED == 'false' || steps.sign.outcome == 'failure' || steps.sign.outcome == 'skipped'
              shell: bash
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              run: |
                  TAG="${GITHUB_REF#refs/tags/}"
                  INSTALLER="${{ steps.find_installer.outputs.INSTALLER_PATH }}"
                  INSTALLER_NAME="${{ steps.find_installer.outputs.INSTALLER_NAME }}"
                  echo "Uploading unsigned: $INSTALLER_NAME"
                  gh release upload "$TAG" "$INSTALLER" --clobber

    # ─────────────────────────────────────────────
    # Linux build
    # ─────────────────────────────────────────────
    build-linux:
        needs: create-release
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup Node
              uses: actions/setup-node@v4
              with:
                  node-version: 22
                  cache: npm

            - name: Install system dependencies
              run: |
                  sudo apt-get update
                  sudo apt-get install -y \
                    libgtk-3-dev \
                    libwebkit2gtk-4.0-dev \
                    libayatana-appindicator3-dev \
                    librsvg2-dev \
                    patchelf

            - name: Update version in package.json
              shell: bash
              run: |
                  VERSION="${GITHUB_REF#refs/tags/v}"
                  CURRENT=$(node -p "require('./package.json').version")
                  if [ "$CURRENT" != "$VERSION" ]; then
                    echo "Updating package.json from $CURRENT to $VERSION"
                    npm version "$VERSION" --no-git-tag-version
                  else
                    echo "package.json already at version $VERSION, skipping."
                  fi

            - name: Install dependencies
              run: npm ci

            - name: Build
              run: npm run build

            - name: Build and publish (Linux)
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              run: npx electron-builder --linux --publish always

    # ─────────────────────────────────────────────
    # Google Chrome Extension build
    # ─────────────────────────────────────────────
    build-chrome-extension:
        needs: create-release
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Install Google Chrome
              shell: bash
              run: |
                  wget -q https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
                  sudo apt-get install -y ./google-chrome-stable_current_amd64.deb

            - name: Package Chrome Extension as CRX
              shell: bash
              run: |
                  # Pack the extension using the existing .pem private key
                  # Output: chrome-extension.crx (placed next to the folder)
                  google-chrome-stable \
                    --pack-extension=chrome-extension \
                    --pack-extension-key=chrome-extension/chrome-extension.pem \
                    --no-sandbox

                  mv chrome-extension.crx viezan-chrome-extension.crx
                  echo "Chrome extension packaged: $(du -sh viezan-chrome-extension.crx | cut -f1)"

            - name: Upload Chrome Extension to GitHub Release
              shell: bash
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              run: |
                  TAG="${GITHUB_REF#refs/tags/}"
                  echo "Uploading Chrome extension to release $TAG"
                  gh release upload "$TAG" viezan-chrome-extension.crx --clobber
