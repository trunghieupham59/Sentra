name: Auto-label PRs

on:
    pull_request_target:
        types: [opened, synchronize, reopened, edited]

permissions:
    contents: read
    pull-requests: write

jobs:
    label-by-path:
        name: Label by file paths
        runs-on: ubuntu-latest
        steps:
            - uses: actions/labeler@v5
              with:
                  repo-token: ${{ secrets.GITHUB_TOKEN }}
                  configuration-path: .github/labeler.yml
                  sync-labels: true

    label-by-size:
        name: Label by PR size
        runs-on: ubuntu-latest
        steps:
            - uses: codelytv/pr-size-labeler@v1
              with:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                  xs_label: "size/xs"
                  xs_max_size: "10"
                  s_label: "size/s"
                  s_max_size: "100"
                  m_label: "size/m"
                  m_max_size: "500"
                  l_label: "size/l"
                  l_max_size: "1000"
                  xl_label: "size/xl"
                  fail_if_xl: "false"
                  message_if_xl: |
                      ⚠️ This PR is **very large** (>1000 LOC). Consider splitting it into smaller, focused PRs to make review easier.
                  files_to_ignore: |
                      package-lock.json
                      yarn.lock
                      pnpm-lock.yaml
