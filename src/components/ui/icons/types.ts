/**
 * Shared prop interface for all icon components.
 * Every icon accepts an optional `className` for sizing / colour overrides via
 * Tailwind utilities (e.g. "w-4 h-4 text-gray-500").
 */
export interface IconProps {
  className?: string
}
